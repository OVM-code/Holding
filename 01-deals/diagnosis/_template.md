# Diagnosis — <company-slug>

Full write-up against `00-thesis/diagnostic-framework.md`. Default posture:
skepticism. This candidate is a value-trap until the evidence says otherwise.

## Evidence for each bucket

Work through all three, even if one looks obviously dominant — the point of
this document is to show what ruled the other two out, not just what
supports the winning label.

### Operator gap

Evidence for / against:

### Cost structure uncompetitive

Evidence for / against:

### Demand structurally evaporating

Evidence for / against:

## Verdict

- **Label:** `operator-gap` / `cost-structure` / `demand-decay`
- **Secondary factor (if mixed):**
- **Confidence:** low / medium / high
- **What would change this verdict:** (the specific piece of evidence that
  would flip it — keeps this falsifiable)

## If savable: what the service-phase engagement should focus on

Only fill in if verdict is `operator-gap` or `cost-structure`. What does the
holding actually need to do inside this business during the service phase to
prove value and de-risk the option? Feeds `02-deal-structure/`.

## If disqualified: what would need to change to re-open this

Only fill in if verdict is `demand-decay` or another hard disqualifier
applied. Keeps the file useful as a future re-check point rather than dead
weight.
